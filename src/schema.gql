# -----------------------------------------------
# !!! THIS FILE WAS GENERATED BY TYPE-GRAPHQL !!!
# !!!   DO NOT MODIFY THIS FILE BY YOURSELF   !!!
# -----------------------------------------------

type Comment {
  Posts: Post
  Users: User
  comment: String!
  createdAt: DateTime!
  id: ID!
}

"""
The javascript `Date` as string. Type represents date and time as the ISO Date string.
"""
scalar DateTime

type Message {
  createdAt: DateTime!
  id: ID!
  receiverId: String!
  senderId: String!
  text: String!
}

input MessageInput {
  receiverId: String!
  senderId: String!
  text: String!
}

type Mutation {
  createPost(author: String!, content: String!): Post!
  deleteOwnPost(postId: Float!): String!
  logIn(email: String!, password: String!): Token
  sendMessage(data: MessageInput!): Message!
  signUp(data: UserSignUp!): User!
}

type Post {
  author: User
  comments: Comment
  content: String!
  createdAt: DateTime!
  id: ID!
}

type Query {
  auth(token: String!): User
  findAllUsers(page: Float!): [User!]
  findUserById(id: String!): User
  getActiveTokens: [Token!]!
  getAllMessagesSent(userId: String!): [Message!]!
  getAllPosts(cursor: Float!): [Post!]!
  getFirstPostsPage: [Post!]!
}

type Subscription {
  messageNotification: Message!
}

type Token {
  id: ID!
  token: String!
  user: User
  usersId: String!
}

type User {
  createdAt: DateTime!
  email: String!
  id: ID!
  name: String
}

input UserSignUp {
  email: String!
  name: String
  password: String!
}
